import { default as dbConfig } from './mongo';

export const controllers = dbConfig.controllers;
export const connect = dbConfig.connect;
export const passport = dbConfig.passport;
export const session = dbConfig.session;
